import { TransformIntensityPipe } from './transform-intensity.pipe';

describe('TransformIntensityPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformIntensityPipe();
    expect(pipe).toBeTruthy();
  });
});
